while True:
    print('Who are you?')
    name = input('>')
    if name != 'Joe':
        print('You are not welcome')
        continue
    print('Hello, Joe. What is the password? (It is a fish.)')
    password = input('>')
    if password == 'swordfish':
        print('That is a pointy fish!')
        break
print('Acess granted.')
